<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.JunazhengDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        a.id as id
        ,a.yonghu_id as yonghuId
        ,a.junazheng_name as junazhengName
        ,a.junazheng_types as junazhengTypes
        ,a.junazheng_file as junazhengFile
        ,a.junazheng_text as junazhengText
        ,a.insert_time as insertTime
        ,a.junazheng_yesno_types as junazhengYesnoTypes
        ,a.junazheng_yesno_text as junazhengYesnoText
        ,a.create_time as createTime
    </sql>
    <select id="selectListView" parameterType="map" resultType="com.entity.view.JunazhengView" >
        SELECT
        <include refid="Base_Column_List" />
        <!-- 级联表的字段 -->
        ,yonghu.yonghu_name as yonghuName
        ,yonghu.yonghu_phone as yonghuPhone
        ,yonghu.yonghu_id_number as yonghuIdNumber
        ,yonghu.yonghu_photo as yonghuPhoto
        ,yonghu.yonghu_email as yonghuEmail
        ,yonghu.yonghu_types as yonghuTypes
        ,yonghu.yonghu_delete as yonghuDelete

        FROM junazheng  a
        left JOIN yonghu yonghu ON a.yonghu_id = yonghu.id

        <where>
            <if test="params.ids != null">
                and a.id in
                <foreach item="item" index="index" collection="params.ids" open="(" separator="," close=")">
                #{item}
                </foreach>
            </if>
            <if test="params.yonghuId != null and params.yonghuId != ''">
                and (
                    a.yonghu_id = #{params.yonghuId}
                )
            </if>
            <if test=" params.junazhengName != '' and params.junazhengName != null and params.junazhengName != 'null' ">
                and a.junazheng_name like CONCAT('%',#{params.junazhengName},'%')
            </if>
            <if test="params.junazhengTypes != null and params.junazhengTypes != ''">
                and a.junazheng_types = #{params.junazhengTypes}
            </if>
            <if test=" params.junazhengText != '' and params.junazhengText != null and params.junazhengText != 'null' ">
                and a.junazheng_text like CONCAT('%',#{params.junazhengText},'%')
            </if>
            <if test=" params.insertTimeStart != '' and params.insertTimeStart != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.insert_time) >= UNIX_TIMESTAMP(#{params.insertTimeStart}) ]]>
            </if>
            <if test=" params.insertTimeEnd != '' and params.insertTimeEnd != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.insert_time) <= UNIX_TIMESTAMP(#{params.insertTimeEnd}) ]]>
            </if>
            <if test="params.insertTime != null and params.insertTime != ''">
                and a.insert_time = #{params.insertTime}
            </if>
            <if test="params.junazhengYesnoTypes != null and params.junazhengYesnoTypes != ''">
                and a.junazheng_yesno_types = #{params.junazhengYesnoTypes}
            </if>
            <if test=" params.junazhengYesnoText != '' and params.junazhengYesnoText != null and params.junazhengYesnoText != 'null' ">
                and a.junazheng_yesno_text like CONCAT('%',#{params.junazhengYesnoText},'%')
            </if>

                <!-- 判断用户的id不为空 -->
            <if test=" params.yonghuIdNotNull != '' and params.yonghuIdNotNull != null and params.yonghuIdNotNull != 'null' ">
                and a.yonghu_id IS NOT NULL
            </if>
            <if test=" params.yonghuName != '' and params.yonghuName != null and params.yonghuName != 'null' ">
                and yonghu.yonghu_name like CONCAT('%',#{params.yonghuName},'%')
            </if>
            <if test=" params.yonghuPhone != '' and params.yonghuPhone != null and params.yonghuPhone != 'null' ">
                and yonghu.yonghu_phone like CONCAT('%',#{params.yonghuPhone},'%')
            </if>
            <if test=" params.yonghuIdNumber != '' and params.yonghuIdNumber != null and params.yonghuIdNumber != 'null' ">
                and yonghu.yonghu_id_number like CONCAT('%',#{params.yonghuIdNumber},'%')
            </if>
            <if test=" params.yonghuEmail != '' and params.yonghuEmail != null and params.yonghuEmail != 'null' ">
                and yonghu.yonghu_email like CONCAT('%',#{params.yonghuEmail},'%')
            </if>
            <if test="params.yonghuTypes != null  and params.yonghuTypes != ''">
                and yonghu.yonghu_types = #{params.yonghuTypes}
            </if>

            <if test="params.yonghuDeleteStart != null  and params.yonghuDeleteStart != '' ">
                <![CDATA[  and yonghu.yonghu_delete >= #{params.yonghuDeleteStart}   ]]>
            </if>
            <if test="params.yonghuDeleteEnd != null  and params.yonghuDeleteEnd != '' ">
                <![CDATA[  and yonghu.yonghu_delete <= #{params.yonghuDeleteEnd}   ]]>
            </if>
            <if test="params.yonghuDelete != null  and params.yonghuDelete != '' ">
                and yonghu.yonghu_delete = #{params.yonghuDelete}
            </if>
            <if test=" params.insertTimeStart != '' and params.insertTimeStart != null ">
                <![CDATA[  and UNIX_TIMESTAMP(yonghu.insert_time) >= UNIX_TIMESTAMP(#{params.insertTimeStart}) ]]>
            </if>
            <if test=" params.insertTimeEnd != '' and params.insertTimeEnd != null ">
                <![CDATA[  and UNIX_TIMESTAMP(yonghu.insert_time) <= UNIX_TIMESTAMP(#{params.insertTimeEnd}) ]]>
            </if>
            <if test="params.insertTime != null and params.insertTime != ''">
                and a.insert_time = #{params.insertTime}
            </if>
        </where>
        order by a.${params.sort} ${params.order}
    </select>

</mapper>